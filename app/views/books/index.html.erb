<!DOCTYPE html>
<html>
  <h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to Show, "/books/#{book.id} " %></td>
      <td><%= link_to Edit, "/books/#{book.id} " %></td>
      <td><%= link_to Destroy, "/books/#{book.id}" %></td>
    </tr>
  </tbody>
</table>
</html>

